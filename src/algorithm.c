
#include "algorithm.h"

// Here in this file you can write C-code (not C++)

int c_example_algorithm(int value) {
    
    // Your C code ...

    return value * 2;
}

void c_init() {
    // Your C-code to run once at the start of the program (load resources)
}

void c_loop() {
    // Your C-code to run repeatedly
}

void c_exit() {
    // Your C-code to run once at the end of the program (release resources)
}
